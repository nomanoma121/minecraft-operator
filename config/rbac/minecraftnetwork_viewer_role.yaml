# This rule is not used by the project minecraft-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to minecraft.nomanoma-dev.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: minecraft-operator
    app.kubernetes.io/managed-by: kustomize
  name: minecraftnetwork-viewer-role
rules:
- apiGroups:
  - minecraft.nomanoma-dev.com
  resources:
  - minecraftnetworks
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - minecraft.nomanoma-dev.com
  resources:
  - minecraftnetworks/status
  verbs:
  - get
